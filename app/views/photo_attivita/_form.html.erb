<%= form_for(@photo_attivitum, html: { multipart:true, class: "form-horizontal", role: "form" }) do |f| %>
  <% if @photo_attivitum.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@photo_attivitum.errors.count, "error") %> prohibited this photo_attivitum from being saved:</h4>

      <ul>
      <% @photo_attivitum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :id_attivita, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :id_attivita, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :titolo, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :titolo, class: "form-control" %>
    </div>
  </div>
     <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
, html:{multipart:true}